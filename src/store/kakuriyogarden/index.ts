import { useState, useEffect, useCallback, useMemo } from 'react'
export const prevData = ()=>{}
export const useCharacterViewModel = ()=>{
  const [, setIsValidError] = useState(false)
  const [isSubmit, setIsSubmit] = useState(false)
}
